import { css } from '@emotion/react';

const styles = css`
  .headEconSec-wrapper {
    & > div > header > div > div {
      border-top: none;
      border-bottom: none;
    }
  }
`;

export default styles;
